# hci-finals — LitFilm

Literary vs film adaptation platform. Bun monorepo with Express + SQLite backend and React + Vite + TypeScript frontend.

## Commands

```bash
bun run dev       # Start server (3001) + client (5173)
bun run build     # Build client for production
bun run seed      # Seed SQLite database
```
